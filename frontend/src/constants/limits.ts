/**
 * TintMatch PRO - Formulation Limits & Science Constants
 * ======================================================
 * Central source of truth for colorant physical limits and industrial tolerances.
 */

/** Maximum total colorant load percentage allowed in base paint (% by weight/volume) */
export const MAX_TOTAL_COLORANT_LOAD = 12.0;

/** Default strict colorimetric tolerance threshold for CIEDE2000 ΔE00 */
export const DEFAULT_TOLERANCE_DE00 = 0.50;

/** Maximum allowed DIN 6172 Metamerism Index */
export const MAX_METAMERISM_INDEX = 0.50;

/** Opacity contrast ratio threshold for full hiding (%) */
export const MIN_OPACITY_RATIO = 98.0;
